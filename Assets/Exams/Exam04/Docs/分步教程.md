# Exam04 分步教程 — 登录注册 + Loading + 移动动画战斗

> **教练宪法**：[`../../Docs/教学规范.md`](../../Docs/教学规范.md) **v7.16.4 · §11.29** · [`../../../Docs/02-学员档案/踩坑记忆库_lixiaotong.md`](../../../Docs/02-学员档案/踩坑记忆库_lixiaotong.md)  
> **清单 + P-L1**：[`教程.md`](教程.md) §一点五 **v3**  
> **档案**：[`学员能力档案_lixiaotong.md`](../../../Docs/02-学员档案/学员能力档案_lixiaotong.md)  
> **开套 P-Score 84** · **S2-** · **递进读码**：Exam02 Auth · Exam06 Loading · Exam03 战斗

> 📋 **Coach 输出前（R1 · §11.28）**：总 Doc → 本文件 **当前大步全文** → **10 项对照**（含 §11.29）→ 再发 Chat。

---

## 使用说明

| 规则 | 说明 |
|------|------|
| **§11.29** | 同域第二套题 · P-L1 **必须含递进条** · 禁止只写「≥ Exam02」 |
| **大步** | Chat「进行下一步」= **一整步**（共 **6 步**） |
| **小分段** | `### N.1`… 在本文件 + **同一条 Chat** |
| **A+B** | 每大步 **📌A + 🔷B**；脚本步 **§11.9 三块** |
| **Chat 改库 K1** | 「进行下一步」= **只教 · 不改仓库** |

---

## 同类题递进对照表（§11.29 · v3 · 2026-07-03）

| 功能域 | 上套 P-L1（已有 · 底线） | **Exam04 P-L1 新增** | P+1 |
|--------|--------------------------|------------------------|-----|
| Auth | `ShowPanel` · Dictionary · OnDestroy | **`SceneNames`** · **Exam04 `AccountData`** · **Trim** · →Loading | 接口拆分 |
| Loading | Start 协程 · 禁 OnEnable-only | **5s 假进度** · **StopCoroutine 对称** | Min+Async 注释 |
| 移动 | `Space.Self` | **`SetBool("IsMoving")`** · 敌人 Idle | CC / BlendTree |
| 战斗 | Instantiate · 开火 · OnDestroy | **注入禁 Find** · **`aliveCount` 全灭** | BulletPool |
| UI | mute | **双反馈** Text/Sprite | Addressables |
| 规范 | 部分 §11.19 | **全脚本五层** | 导图 png |

---

## 本套 P+1 总表（v3 · 已剔除升格进 P-L1 的项）

| # | P+1 | 维度 | ☐ |
|:-:|-----|------|:-:|
| 1 | Loading 注释 **Min(fake,real/0.9f)** 与 Exam05/06 差异 | D4 | ☐ |
| 2 | **CharacterController** 替代 Translate | U11 | ☐ |
| 3 | **BlendTree** 八向（W3 Lab 预习） | K2 | ☐ |
| 4 | **BulletPool** 对象池 | D6 | ☐ |
| 5 | Build 断网 · `Exam04_思维导图.png` | D6 | ☐ |

**已升格 P-L1（不算 P+1）**：AccountData · SceneNames · SetBool · 注入 · aliveCount · §11.19 全脚本 · mute 双反馈

---

## 大步与试卷对照表

| 大步 | 试卷分点 | 小分段 | 状态 |
|:----:|----------|--------|:----:|
| **第 1 步** | 1 · 工程 | 1.1 读题递进 · 1.2 三场景+Build | **🔒 ← 当前** |
| 第 2 步 | 2 · Auth | 2.1 Prefab 体检 · 2.2 AccountData+AuthController | 🔒 |
| 第 3 步 | 3 · Loading | 3.1 UI · 3.2 LoadingController | 🔒 |
| 第 4 步 | 4～5 · 移动动画 | 4.1 Main 场景 · 4.2 Player+Animator+刷怪 | 🔒 |
| 第 5 步 | 6～9 · 战斗 | 5.1 BattleUI · 5.2 子弹+GameManager | 🔒 |
| 第 6 步 | 10 · 交付 | 6.1 注释 · 6.2 Build 断网 | 🔒 |

---

## 大步路线图

```
第1步  读题+导图(3递进点)+三场景+Build 0→1→2
  ↓
第2步  LoginUI Prefab · AccountData · AuthController · SceneNames
  ↓
第3步  LoadingCanvas · LoadingController 5s → Main
  ↓
第4步  Player WASD+SetBool · 3敌 · 敌人Idle
  ↓
第5步  音乐/开火/关闭 · Bullet · aliveCount 胜利
  ↓
第6步  §11.19 全脚本 · Build 断网 · 交付
```

---

## 第 1 步：读题 + 工程与三场景（考点 1）

> 状态：**🔒 待学员完成** · **← 当前**  
> **小分段**：1.1 读题·递进导图 · 1.2 三场景+Build

### 1.1 读题 · 递进思维导图

| # | 操作 |
|:-:|------|
| 1 | 通读 [`教程.md`](教程.md) §一 + **§一点五 v3** |
| 2 | 画导图：**Login → Loading 5s → Main**（动画+三钮+全灭） |
| 3 | 导图**必须标注 3 个递进点**（相对 Exam02/03）：Auth 串联 Loading · 5s 假进度 · 动画+aliveCount |
| 4 | 草图即可 · 第 6 步导出 `Exam04_思维导图.png` |

**与 Exam02/03/06 差异（口试）**

| 项 | 上套 | **Exam04** |
|----|------|------------|
| 场景 | 2～5 个 | **3** 固定链路 |
| Loading | Async/无/6s | **5s 假进度** |
| 战斗胜负 | 加血/扣血/无 | **敌人全灭计数** |
| 动画 | 无 | **IsMoving + 敌人 Idle** |

### 1.2 三场景 + Build

| # | 操作 | 验收 |
|:-:|------|------|
| 1 | 建 `Scenes/Exam04_LoginScene.unity` | 可打开 |
| 2 | 建 `Scenes/Exam04_LoadingScene.unity` | 可打开 |
| 3 | 建 `Scenes/Exam04_MainScene.unity` | 可打开 |
| 4 | Build Settings：**0 Login · 1 Loading · 2 Main** | Exam04 **置顶** |
| 5 | 建空目录 `Scripts/`（本步仍不写脚本） | 存在即可 |

**Hierarchy（本步空场景）**

```
Exam04_LoginScene          Exam04_LoadingScene       Exam04_MainScene
├── Main Camera            ├── Main Camera           ├── Main Camera
└── Directional Light      └── Directional Light     └── Directional Light
```

### 🎯 L1 / P-L1 / P+1（本大步）

| 档 | 要求 |
|----|------|
| **L1** | 三 `.unity` · Build 0→1→2 |
| **P-L1** | 导图含 **3 递进点** · 能口述 **vs Exam02/03** 各 1 条 |
| **P+1** | 导图加 **四套对比小表**（02/03/06/04） |

### 本步验收

- [ ] 三场景存在  
- [ ] Build **0=Login 1=Loading 2=Main** · **Index 0 不是 Exam01/03**  
- [ ] 导图标注：**SceneNames 规划 · 5s Loading · 动画+全灭**

### 📌 原理加餐（A）

> **U16** · **U03**

- **是什么**：Exam04 把 Exam02 **Auth 链**、Exam06 **协程 Loading**、Exam03 **战斗 Instantiate** 合成 **一条三段式流水线**。  
- **卷面为何**：考点 3 强制独立 Loading 场景；考点 5 强制 Animator。  
- **再挖一层**：Build Index 0 必须是 Login — 否则打包后跳过注册（踩坑 L2 同类）。

### 🔷 工程进阶 B

**FICM · 工程骨架**

| 维 | 试卷 L1 | 你现状 | **Exam04 递进** |
|:--:|---------|--------|-----------------|
| ① 职责 | 三场景文件 | Exam03 做过 2 场景 | **+Loading 中间场景** |
| ② 数据 | — | Exam02 AccountData | 第 2 步 **Exam04 命名空间复制** |
| ⑤ 状态 | Build 顺序 | 踩坑 L2 | **断网从 Index 0 测** |
| ⑧ 可维护 | 场景名 | Exam02 字符串散落 | 第 2 步 **`SceneNames` 类** |

**B本步 · 对照代码**（第 2 步创建 · 本步先记名）

```csharp
namespace Exam.Exam04
{
    public static class SceneNames
    {
        public const string Login = "Exam04_LoginScene";
        public const string Loading = "Exam04_LoadingScene";
        public const string Main = "Exam04_MainScene";
    }
}
```

### 进度（第 1 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 2 步：登录注册（考点 2）

> 状态：**🔒 待学员完成**  
> **§11.29 递进**：Exam02 底线之上 · **+ SceneNames + Exam04 AccountData + Trim + 进 Loading**

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| 2.1 | Prefab 体检 · 场景拖 Prefab | Login/Register 互斥 |
| 2.2 | `SceneNames` · `AccountData` · `AuthController` | 登录 → Loading |

---

### Prefab 体检（§11.13 · O1d）

| 物体/组件 | Prefab 现状 | 问题 | Exam04 改法 |
|-----------|-------------|------|-------------|
| `LoginUI` 根 | Canvas Scale=0 | 有 CanvasScaler 正常 | 保留 |
| 根脚本 | 旧 `LoginUI` 脚本 | 可能 Missing/错场景名 | **Remove** · 改挂 **AuthManager** |
| `LoginPanel` | 默认 **Active** | — | 入口面板 |
| `RegisterPanel` | 子面板 | — | 默认 **关** |
| `LoginButton` | 登录钮 | — | 绑 AuthController |
| `BtnRegister` | 打开注册 | — | ShowPanel(register) |
| `BtnExit` | 退出 | 卷面可选 | `Application.Quit` / Editor 停 Play |
| `AccountInput`/`PasswordInput` | 两套 | 密码 ContentType | **Password** |
| `CreatRoleUI.prefab` | 存在 | 本卷不用 | 不拖场景 |

**Hierarchy（LoginScene 目标）**

```
Exam04_LoginScene
├── Main Camera
├── Directional Light
├── EventSystem
├── AuthManager          ← AuthController + AccountData（或子物体 AccountData）
└── LoginUI (Prefab)
    └── Background
        ├── LoginPanel   ← 默认开
        └── RegisterPanel ← 默认关
```

### 2.1 场景操作

| # | 操作 |
|:-:|------|
| 1 | 打开 `Exam04_LoginScene` |
| 2 | 拖 `Prefabs/LoginUI.prefab` |
| 3 | UI → Event System（若无） |
| 4 | 空物体 **`AuthManager`** |
| 5 | Prefab 根 **Remove** 旧脚本（若有 Missing） |

#### `LoginUI` · Canvas Scaler

| 字段 | 设置值 | 含义 |
|------|--------|------|
| UI Scale Mode | Scale With Screen Size | 分辨率自适应 |
| Reference Resolution | **1920 × 1080** | 参考分辨率 |
| Match | **0.5** | 宽高折中 |

---

### 2.2 脚本（§11.9 三块）

#### 本段思路

1. 建 **`SceneNames`** 静态类 — 全套 LoadScene 只读常量。  
2. **复制** Exam02 `AccountData.cs` → `Exam04/Scripts/` · 改 **`namespace Exam.Exam04`** · 挂 **AuthManager** 子物体 **`AccountData`**。  
3. **`AuthController`**：`ShowPanel` 互斥 Login/Register · Register/Login 调 **Trim** 后 Dictionary · 成功 **`LoadScene(SceneNames.Loading)`**。

#### 带注释 P-L1 范本

```csharp
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 登录注册：两面板互斥、AccountData 校验、成功后进 Loading。
    /// 递进：相对 Exam02 增加 SceneNames + 强制 Trim + 跳转 Loading 非 Main。
    /// </summary>
    public class AuthController : MonoBehaviour
    {
        // --- UI 面板（互斥）---
        [SerializeField] private GameObject loginPanel;
        [SerializeField] private GameObject registerPanel;

        [Header("Login")]
        [SerializeField] private InputField loginAccountInput;
        [SerializeField] private InputField loginPasswordInput;
        [SerializeField] private Button btnLogin;
        [SerializeField] private Button btnRegister;
        [SerializeField] private Button btnExit;

        [Header("Register")]
        [SerializeField] private InputField registerAccountInput;
        [SerializeField] private InputField registerPasswordInput;
        [SerializeField] private Button btnConfirmRegister;
        [SerializeField] private Button btnBackRegister;

        private void Awake()
        {
            ShowPanel(loginPanel);
        }

        private void Start()
        {
            btnRegister.onClick.AddListener(() => ShowPanel(registerPanel));
            btnBackRegister.onClick.AddListener(() => ShowPanel(loginPanel));
            btnConfirmRegister.onClick.AddListener(OnRegisterClick);
            btnLogin.onClick.AddListener(OnLoginClick);
            btnExit.onClick.AddListener(() => Application.Quit());
        }

        /// <summary>思路：同一时刻只亮一个面板，禁止两个 SetActive(true)。</summary>
        private void ShowPanel(GameObject target)
        {
            loginPanel.SetActive(target == loginPanel);
            registerPanel.SetActive(target == registerPanel);
        }

        private void OnRegisterClick()
        {
            string account = registerAccountInput.text.Trim();
            string password = registerPasswordInput.text.Trim();
            if (AccountData.Instance.Register(account, password, out string msg))
            {
                registerAccountInput.text = "";
                registerPasswordInput.text = "";
                ShowPanel(loginPanel);
            }
            Debug.Log(msg);
        }

        private void OnLoginClick()
        {
            string account = loginAccountInput.text.Trim();
            string password = loginPasswordInput.text.Trim();
            if (AccountData.Instance != null && AccountData.Instance.TryLogin(account, password))
            {
                SceneManager.LoadScene(SceneNames.Loading);
                return;
            }
            Debug.Log("登录失败");
            loginAccountInput.text = "";
            loginPasswordInput.text = "";
            loginAccountInput.ActivateInputField();
        }

        private void OnDestroy()
        {
            if (btnLogin != null) btnLogin.onClick.RemoveListener(OnLoginClick);
            if (btnRegister != null) btnRegister.onClick.RemoveAllListeners();
            if (btnConfirmRegister != null) btnConfirmRegister.onClick.RemoveListener(OnRegisterClick);
            if (btnBackRegister != null) btnBackRegister.onClick.RemoveAllListeners();
            if (btnExit != null) btnExit.onClick.RemoveAllListeners();
        }
    }
}
```

#### 逐行讲解表

| 行/块 | 含义 |
|-------|------|
| `SceneNames.Loading` | **递进**：Exam02 直进 Server/Game · 本卷先进 Loading |
| `.Trim()` | **递进**：AuthController 侧再 Trim（AccountData 内也有） |
| `ShowPanel` | §七 已掌握 · **禁止** ShowLogin/ShowRegister 两方法 |
| `OnDestroy` | §七 · **RemoveListener(命名方法)** 优先于 RemoveAllListeners |
| `AccountData` | 复制 Exam02 · **仅改 namespace** · DDOL 单例 |

#### Inspector 拖引用

| SerializeField | 拖 Prefab 内谁 |
|----------------|----------------|
| loginPanel | `LoginPanel` |
| registerPanel | `RegisterPanel` |
| loginAccountInput | LoginPanel → `AccountInput` |
| loginPasswordInput | LoginPanel → `PasswordInput` |
| btnLogin | `LoginButton` |
| btnRegister | `BtnRegister` |
| registerAccountInput | RegisterPanel → `AccountInput` |
| registerPasswordInput | RegisterPanel → `PasswordInput` |
| btnConfirmRegister | RegisterPanel 确认钮 |
| btnBackRegister | `BtnBackRegister` |

### 🎯 L1 / P-L1（本大步）

| 档 | 要求 |
|----|------|
| **L1** | 注册后可登录 |
| **P-L1** | **AccountData** · **Trim** · **SceneNames.Loading** · **ShowPanel** · **OnDestroy** |

### 本步验收

- [ ] 注册 → 回 LoginPanel → 可登录  
- [ ] 登录成功进 **Loading 场景**（可先空场景）  
- [ ] **无**写死 `if (account=="xxx")`  
- [ ] Prefab **无 Missing Script**

### 📌 原理加餐（A）

- **U17**：Dictionary 键账号值密码 — 学院月考高频。  
- **递进**：Exam02 五场景链 · 本套 **三场景** 更短，但 **Auth 代码规范不能降**。

### 🔷 工程进阶 B

**B0**：Exam02 `LoginUIController` + `AccountData`  
**B本步**：`SceneNames` 集中管理 · Auth 不直接写 `"Exam04_LoadingScene"` 字面量  
**B下一档（P+1）**：`IAuthService` 接口 + Mock（阶段 02）

### 进度（第 2 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 3 步：Loading 5 秒（考点 3）

> 状态：**🔒 待学员完成**  
> **§11.29 递进**：Exam06 **L3** → **Start 启协程** · 禁 OnEnable-only · **StopCoroutine 对称**

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| 3.1 | LoadingCanvas + Slider | 0～1 不可拖 |
| 3.2 | LoadingController | **5 秒**满 → Main |

---

### 3.1 Loading UI

打开 **`Exam04_LoadingScene.unity`**。

| # | 操作 |
|:-:|------|
| 1 | UI → Canvas → **`LoadingCanvas`** |
| 2 | Event System |
| 3 | Slider → **`ProgressSlider`** |
| 4 | Text → **`TipText`**（「加载中…」） |

#### `LoadingCanvas` · Canvas Scaler

| 字段 | 设置值 | 含义 |
|------|--------|------|
| Reference Resolution | **1920 × 1080** | 与 Login 一致 |
| Match | **0.5** | 折中缩放 |

#### `ProgressSlider`

| 字段 | 设置值 | 含义 |
|------|--------|------|
| Min / Max | **0 / 1** | 百分比 |
| Value | **0** | 初始空 |
| Interactable | **关** | 脚本驱动 |
| Handle Rect | **None** | 无手柄 |
| Fill Rect | **Fill** | 子物体 Fill |

| RectTransform | Anchor 底边中 · Width **900** · Height **40** · Pos Y **120** |

---

### 3.2 LoadingController

#### 本段思路

1. **Start** → `StartCoroutine(LoadProgress())`（**禁止**仅 OnEnable）。  
2. `elapsed` 累加 · `slider.value = elapsed/5f`。  
3. 满 5 秒 → `LoadScene(SceneNames.Main)`。  
4. **OnDestroy** → `StopCoroutine` 防泄漏。

#### 带注释 P-L1 范本

```csharp
using System.Collections;
using UnityEngine;
using UnityEngine.SceneManagement;
using UnityEngine.UI;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 Loading：5 秒假进度条，满后进入 Main。
    /// 递进：结构对齐 Exam06（Start 协程），卷面不要求 Async/Min 双轨。
    /// </summary>
    public class LoadingController : MonoBehaviour
    {
        [SerializeField] private Slider progressSlider;
        [SerializeField] private float duration = 5f;

        private Coroutine loadRoutine;

        private void Start()
        {
            progressSlider.value = 0f;
            loadRoutine = StartCoroutine(LoadProgress());
        }

        /// <summary>思路：每帧 elapsed 累加，Slider 0→1，满 duration 跳转 Main。</summary>
        private IEnumerator LoadProgress()
        {
            float elapsed = 0f;
            while (elapsed < duration)
            {
                elapsed += Time.deltaTime;
                progressSlider.value = Mathf.Clamp01(elapsed / duration);
                yield return null;
            }
            SceneManager.LoadScene(SceneNames.Main);
        }

        private void OnDestroy()
        {
            if (loadRoutine != null)
                StopCoroutine(loadRoutine);
        }
    }
}
```

#### 逐行讲解表

| 行/块 | 含义 |
|-------|------|
| `Start` + 存 `loadRoutine` | **递进** · 踩坑 L3 · 对称 Stop |
| 纯 `elapsed/duration` | 卷面 **5s 假进度** · 非 Exam05 Min 双轨 |
| `SceneNames.Main` | 与 SceneNames 类一致 |

### 🎯 L1 / P-L1

| **L1** | **P-L1** |
|--------|----------|
| 约 5 秒 Slider 满进 Main | **Start 协程** · **StopCoroutine** · Play 从 **Login 链** 测 |

### 本步验收

- [ ] Login → Loading → **≈5 秒** → Main（Main 可先空）  
- [ ] Slider 0→1 平滑  
- [ ] **非** OnEnable-only 启协程

### 📌 原理加餐（A）

- **U03**：`yield return null` 每帧刷新 UI。  
- **vs Exam06**：6s+可静音 · 本卷 **5s** 且无 Async — **P+1** 可注释 Min 对照。

### 🔷 工程进阶 B

**B本步**：假进度 = 时间遮罩 · 行业常 **Max(time, asyncProgress)** — 本卷 P+1  
**B下一档**：`LoadingConfig` ScriptableObject（小灶/阶段 02）

### 进度（第 3 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 4 步：Main 移动 + 动画 + 刷怪（考点 4～5）

> 状态：**🔒 待学员完成**  
> **§11.29 递进**：Exam03 移动 + **`SetBool("IsMoving")`** · 敌人 **Idle Animator**

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| 4.1 | Plane · Player · GameManager | 场景可见 |
| 4.2 | PlayerController · Animator · 3 敌 | 动停动画 · 3 敌 Idle |

---

### 4.1 Main 场景摆放

| # | 操作 |
|:-:|------|
| 1 | 拖 `Exam03/Prefabs/Plane.prefab` → (0,0,0) |
| 2 | 建 **`Player`**：Capsule/Cube + **Animator** + `PlayerController` |
| 3 | 空物体 **`GameManager`** + `GameManager`（刷怪） |
| 4 | 复制/拖 **`CubeEnemy.prefab`** 到 Project `Exam04/Prefabs/Enemy.prefab` |
| 5 | Enemy 加 **Animator**（Idle 剪辑循环） |

**Hierarchy**

```
Exam04_MainScene
├── Main Camera
├── Directional Light
├── Plane
├── Player (+ Animator + PlayerController)
├── GameManager (+ GameManager)
└── （运行时 3× Enemy）
```

---

### 4.2 Animator 设置（Player）

| 项 | 值 |
|----|-----|
| Controller | 新建 `PlayerAnimator.controller` |
| 参数 | **`IsMoving`** Bool |
| 状态 | **Idle**（默认）↔ **Run** |
| 转移条件 | `IsMoving == true` → Run · `false` → Idle |

**敌人 Enemy**：单 **Idle** 状态循环即可（卷面「待机动画」）。

---

### 4.2 PlayerController + GameManager 范本

#### PlayerController · 本段思路

1. WASD → `Translate(..., Space.Self)`（§七 · Exam03 已有）。  
2. `bool moving = |h|+|v| > 0` → **`animator.SetBool("IsMoving", moving)`**。

```csharp
using UnityEngine;

namespace Exam.Exam04
{
    /// <summary>
    /// Exam04 主角：WASD 移动 + IsMoving 驱动 Idle/Run。
    /// </summary>
    public class PlayerController : MonoBehaviour
    {
        [SerializeField] private float moveSpeed = 6f;
        [SerializeField] private float turnSpeed = 90f;
        [SerializeField] private Animator animator;

        private static readonly int IsMovingHash = Animator.StringToHash("IsMoving");

        private void Update()
        {
            float h = Input.GetAxis("Horizontal");
            float v = Input.GetAxis("Vertical");

            transform.Translate(new Vector3(0, 0, v) * moveSpeed * Time.deltaTime, Space.Self);
            transform.Rotate(0f, h * turnSpeed * Time.deltaTime, 0f);

            bool moving = Mathf.Abs(h) > 0.01f || Mathf.Abs(v) > 0.01f;
            animator.SetBool(IsMovingHash, moving);
        }
    }
}
```

#### GameManager 刷怪（本步仅刷怪 · 第 5 步加 aliveCount）

```csharp
/// <summary>思路：Start 循环 3 次 Instantiate，XZ Random 散布。</summary>
private void SpawnEnemies()
{
    for (int i = 0; i < enemyCount; i++)
    {
        float x = Random.Range(-spawnRange, spawnRange);
        float z = Random.Range(-spawnRange, spawnRange);
        Instantiate(enemyPrefab, new Vector3(x, spawnY, z), Quaternion.identity);
    }
}
```

### 🎯 L1 / P-L1

| **L1** | **P-L1** |
|--------|----------|
| WASD · 3 敌 · 动画可见 | **`SetBool`** · **`Space.Self`** · 敌人 **Idle** 循环 |

### 本步验收

- [ ] 站定 **Idle** / 移动 **Run**  
- [ ] **3 敌人** 随机位置  
- [ ] 敌人 **Idle** 动画播放

### 📌 原理加餐（A）

- **U06/U07**：Bool 参数 + 转移 — W3 Lab 同构。  
- **`StringToHash("IsMoving")`**：性能习惯 · P-L1 可用字符串 overload。

### 🔷 工程进阶 B

**B0**：Exam03 `TrankController` 仅移动  
**B本步**：动画层 **表现与逻辑分离** — 移动代码不直接 Play 动画名  
**B下一档（P+1）**：BlendTree 八向 · CharacterController

### 进度（第 4 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 5 步：战斗 UI + 子弹 + 全灭胜利（考点 6～9）

> 状态：**🔒 待学员完成**  
> **§11.29 递进**：禁 **FindObjectOfType** · **`aliveCount`** · mute **双反馈**

| 小分段 | 做什么 | 验收 |
|:------:|--------|------|
| 5.1 | PlayerPanel 三钮 UI | 音乐/开火/关闭 |
| 5.2 | Bullet · Enemy 死亡 · GameManager 胜利 | 全灭 Text |

---

### 5.1 主角面板 UI

| 物体 | 说明 |
|------|------|
| `PlayerPanel` | Screen Space Overlay 子 Canvas 或 World Space |
| `BtnMusic` | 一键 mute · **Text「音乐:开/关」** 双反馈 |
| `BtnFire` | Instantiate Bullet |
| `BtnClose` | `playerPanel.SetActive(false)` **只关面板** |

**禁止**：两个钮分别开/关音乐。

---

### 5.2 脚本思路

#### GameManager

1. `SpawnEnemies` 时 `aliveCount = 3` · 每个敌人 **`Init(this)`** 注入。  
2. **`OnEnemyDied()`** → `aliveCount--` · 0 时 **`victoryText.SetActive(true)`**。

#### EnemyController（递进 vs Exam03）

- **禁止** `FindObjectOfType`  
- **`[SerializeField] int hp`** · 子弹 Trigger → hp-- → 0 时 **`gameManager.OnEnemyDied()`** + Destroy self

#### BattleUIController

- mute 切换 + **更新 Text**  
- Fire → Instantiate + AddForce  
- Close → 隐藏 Panel  
- **OnDestroy** 解绑

```csharp
// BattleUIController · mute 双反馈片段
private void ToggleMusic()
{
    isMuted = !isMuted;
    bgmSource.mute = isMuted;
    musicStateText.text = isMuted ? "音乐:关" : "音乐:开";
}
```

### 🎯 L1 / P-L1

| **L1** | **P-L1** |
|--------|----------|
| 三钮 · 子弹 · 全灭胜利 | **注入 GameManager** · **aliveCount** · **双反馈** · **OnDestroy** |

### 本步验收

- [ ] 音乐一键 + **文案变化**  
- [ ] 开火有子弹 · 敌人 Destroy · **aliveCount 减**  
- [ ] 关闭只藏 Panel · 游戏继续  
- [ ] **3 敌全灭** → 胜利 Text  
- [ ] 代码 **无** FindObjectOfType

### 📌 原理加餐（A）

- **U19**：胜负 = **计数器** · 与 Exam03 **加血胜利** 不同 — 卷面别混。  
- **U10**：子弹 Instantiate — Exam03 已有 · 本套加 **敌人死亡回调**。

### 🔷 工程进阶 B

**FICM · 战斗包**

| 维 | Exam03 你现状 | **Exam04 P-L1** |
|:--:|---------------|-----------------|
| ② 数据 | FindObjectOfType 加血 | **SerializeField 注入 GameManager** |
| ⑤ 状态 | 加血满条胜利 | **aliveCount == 0** |
| ③ UI | 开火钮 | + **mute 双反馈** + 关闭 Panel |

**模块完成**：Play 跑通后 Coach 问 **`小灶：Exam04战斗`** 或 **`跳过小灶`**（§11.24）。

### 进度（第 5 步）

| L1 | P-L1 | A | B |
|:--:|:----:|:---:|:---:|
| ☐ | ☐ | ☐ | ☐ |

---

## 第 6 步：注释 + Build 断网交付（考点 10）

> 状态：**🔒 待学员完成**

### 6.1 §11.19 注释自检

| # | 检查 |
|:-:|------|
| 1 | 每类 **类顶 summary** + 方法 **思路注释** |
| 2 | 字段 **`[Header]` 分区** |
| 3 | 删 **unused using**（如 VisualScripting） |
| 4 | 全脚本 **`namespace Exam.Exam04`** |

### 6.2 Build 断网

| # | 项 |
|:-:|-----|
| 1 | Build **仅 Exam04** 三场景 **0→1→2** |
| 2 | 断网全流程 Play |
| 3 | `Exam04_思维导图.png` → `Docs/` |
| 4 | Chat **「Exam04 交付完成」** |

### 本步验收

- [ ] 断网无报错  
- [ ] **§11.19** 全脚本过  
- [ ] 递进项 **§一点五 v3** 全 ✅

### 📌 原理加餐（A）

- **U16**：工程隔离 + 可读性 = 主程基本功。

### 🔷 工程进阶 B

**B本步**：交付清单 = CI 最小门禁 · 行业 Build 还有 **Script Compilation + 资源校验**（阶段 02）。

### 进度（第 6 步）

| L1 | P-L1 | 备注 |
|:--:|:----:|------|
| ☐ | ☐ | 交付 |

---

## 断网后 · 档案六步（§11.7）

> **「Exam04 交付完成」** 或 **开 Exam05** → Coach 同一轮写入四文件。

## 当前进度表

| 大步 | L1 | P-L1 | A | B | 备注 |
|------|:--:|:----:|:-:|:-:|------|
| **第 1 步** | ☐ | ☐ | ☐ | ☐ | **← 当前** |
| 第 2 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 3 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 4 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 5 步 | ☐ | ☐ | ☐ | ☐ | |
| 第 6 步 | ☐ | ☐ | — | — | 交付 |
